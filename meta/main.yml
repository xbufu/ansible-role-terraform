galaxy_info:
  role_name: terraform
  author: xbufu
  description: Terraform for Debian/Ubuntu.
  company: Bufu-Security
  license: license (BSD, MIT)
  min_ansible_version: "2.1"
  platforms:
    - name: Debian
      versions:
        - all
    - name: Ubuntu
      versions:
        - all

  galaxy_tags:
    - system
    - development
    - devops
    - automation
    - terraform
    - hashicorp

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
